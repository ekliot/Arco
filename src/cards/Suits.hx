enum Suits{
    Blades; // damage finisher
    StdBlades( rank : Int );
    Bones; // discard finisher
    StdBones( rank : Int );
    Stars; //draw finisher
    StdStars( rank : Int );
    Stones; // heal finisher
    StdStones( rank : Int );
    Joker; // special
}
